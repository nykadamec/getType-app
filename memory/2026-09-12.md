# 2026-09-12

## getType AI post-processing — full cycle done <!-- project: github.com/nykadamec/getType-app -->
- Brainstormed (skill brainstorming) → user chose: new features, AI actions + pill control, lightweight layer.
- Plan `docs/plans/2026-09-12-ai-settings-sidebar.md` (writing-plans-enhanced, 9 tasks), approved by user, mirrored to Obsidian vault **Nykadamec** at `getType/Plans/2026-09-12 AI settings sidebar.md` (+ 2 PNG mockups embedded). User said: plans for approval go to Obsidian so he can see them there.
- Mockups in `gettype.pen` (pen MCP, frames `ZnaFT` "08 Settings — AI", `MRY1X` "09 Settings — Model", exports in `designs/`). Decisions: AI pane all-English copy; "AI model" select lives on Model page, whisper renamed to "Speech model"; unchecked checkbox = white fill + border (transparent was invisible).
- Implemented subagent-driven (1 subagent per task, review between), all committed: cdfadc8, b4d0b7a, 4230d74, 44ab3df, 470ce5e, fed8645, 64dc412, 6e44cdf, 527715d.
- Architecture: `llm.rs` (Groq chat, temp 0.2, 15s timeout, `<transcript>` wrapping), hook in `stt.rs` with raw fallback, `ai_enabled` flag default OFF, same Groq key. Actions: Direct (passthrough), Cleanup, Summarize, Translate-to-EN. Quick actions in pill.
- Remaining: manual smoke test with mic + Groq key (user-side). Obsidian plan deliberately NOT marked implemented (user: "nemusíš zatím").

## getType26 — nový vzhled (mockup, pen) <!-- project: github.com/nykadamec/getType-app -->
- Zdroj: `~/Documents/gettype26.pen` (otevřený v Pen.app), nové framy `NL · Pills` / `NL · Popover` / `NL · Settings`, exporty `designs/nl/*.png`.
- Směr: monochromní minimalistický (žádná fialová/gradienty) — plochy `#141416`/`#333333`, text `#F5F5F5`/`#A8A8A8`/`#828282`, aktivní prvek = inverzní bílá (jako dark theme v appce).
- Pily: všechny 148×36/radius 18 (idle = `Hold ⌥ Space` keycap nápověda, ten se líbí nejvíc → vzor pro ostatní). Transcribing místo 3 velkých teček tenká track + bílý progress.
- Popover musí kopírovat reálný `src/popover.html` (waveform + getType, gear, ● Ready, hint, segmented Push to talk/Toggle, model + keycapy), ne transcript kartu.
- Settings: rail bez loga pod window controls, karty v jednom `Cards` framu s gapem 16, okno 900×720.
- Ponechané bokem: `~/Documents/gettype26-light.*` z CLI běhu (jen přebarvení) — nahrazeno.
- Pily finálně 134×32, radius 16, text 10,5–11 px (reference: reálné diktovací pilulky jsou malé, Wispr Flow ~70 px). Vzor = idle (`Hold ⌥ Space`).
- Edge fade: v každé pile dva 16px rectangly `layoutPosition:"absolute"` (x 1 / x 117, y 1, h 30, cornerRadius kopíruje vnitřní zaoblení) s gradientem `#333333` → `#333333D9` (0,5) → `#33333373` (0,8) → `#33333300`. rotation 270 pro levý, 90 pro pravý.
- Zkoušel jsem per-char oblouk (každé písmeno zvlášť s rotací a y offsetem) — Dominik odmítl: "ten text vypadá hroznď, chci ho normálnď jako normálnď text, nedávej text na každé písmeno". Zůstává jediný text node + fade.
- **Layout past:** děti s `layoutPosition:"absolute"` ve flex rámci POŘÁD zabírají místo v layoutu → timer a `esc` se překrývaly, dokud jsem fade nepřesunul do vlastního overlay rámce. Správná struktura pily: `Pill { layout:"none" }` → `Content { layout:"horizontal", padding, gap }` + `Overlay { layout:"none" }` s fade rectangly. Všechny tři pily takto přestavěny.
- `Export` chce id jako string v poli — `Export([var],…)` padá na "is not defined".
- Waveform (recording): 8 pruhů, šířka 2,5, gap 4,5, výšky [11,17,21,14,19,13,16,10] (víc variabilní, méně tapering), rámec `width:52`. Pilulky zůstávají 134×32.
- Pozor: `Insert` selhává s "No parent for node" v subtreech, kde předtím proběhl rollback — pomáhá subtree smazat a postavit znovu. `Delete` + `Insert` v jednom `execute` volání je rizikové, dělat zvlášť. `.forEach(id=>...)` občas padá na "cannot read property of undefined" — radši explicitní volání.

## User preference: ask_user for questions
- User asked openclaw agent to use ask-user tool (https://docs.openclaw.ai/tools/ask-user) whenever asking. Added to global `AGENTS.md` ("Asking Questions — use ask_user"). Applies to brainstorming/plan flows too — always call `ask_user` for decisions instead of plain-text A/B/C.
