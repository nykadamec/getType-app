{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popover",
  "description": "Capability for the tray popover window (invoke + focus events)",
  "windows": ["popover"],
  "permissions": ["core:default", "core:event:default"]
}
